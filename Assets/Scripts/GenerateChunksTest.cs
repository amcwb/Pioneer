using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateChunksTest : MonoBehaviour
{
    //public GameObject chunkGenerator;
    //public int Chunks;
    //public int ChunkWidth;
    //public int Seed;

    //public int CurrentChunkOffset = 0;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    InvokeRepeating("Generate", 1.0f, 0.2f);
    //}

    //void Generate()
    //{
    //    GameObject newChunk = Instantiate(chunkGenerator, new Vector3(ChunkWidth * CurrentChunkOffset, 0f), Quaternion.identity);
    //    newChunk.GetComponent<GenerateChunkTest>().Seed = Seed;
    //    newChunk.GetComponent<GenerateChunkTest>().ChunkCoordinateOffset = CurrentChunkOffset;

    //    CurrentChunkOffset++;
    //}
}
